schema: https://graphql.mainnet.sui.io/graphql
documents: './queries/*.graphql'
generates:
  ./generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
    config:
      useTypeImports: true
